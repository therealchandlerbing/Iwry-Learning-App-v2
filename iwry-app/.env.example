# Iwry Portuguese Learning App - Environment Variables
#
# For production: Set these in the Vercel dashboard under Settings > Environment Variables
# For local dev: Copy this to .env.local and fill in your values

# Google Gemini API Key (Required)
# Get your API key from: https://aistudio.google.com/apikey
# In Vercel: Add as GEMINI_API_KEY in Environment Variables
GEMINI_API_KEY=your_gemini_api_key_here

# PostgreSQL Database (Required)
# These are automatically set when you connect a Postgres database in Vercel Storage
# To set up:
#   1. Go to your Vercel project dashboard
#   2. Navigate to Storage tab
#   3. Create a new Postgres database (Neon)
#   4. Connect it to your project with prefix "POSTGRES"
#   5. Vercel will automatically inject these variables
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# NextAuth Configuration (Required for authentication)
# Generate a random secret: openssl rand -base64 32
AUTH_SECRET=your_generated_secret_here

# NextAuth URL (Required)
# For production: Your Vercel deployment URL
# For local dev: http://localhost:3000
NEXTAUTH_URL=https://your-app.vercel.app
